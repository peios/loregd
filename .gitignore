# Local build output from `go build`.
/loregd

# Local SQLite hive files created during manual testing.
*.db
*.db-*
*.regdb
*.regdb-*
*.sqlite
*.sqlite-*
*.sqlite3
*.sqlite3-*

# Ad-hoc hive files left by earlier local runs.
/A
/internal/hivedb/My

/out

# Production builds materialise the go.sum-locked graph in a Pekit stage. Keep
# an ad-hoc local `go mod vendor` from becoming source-tree payload by accident.
/vendor

# Python metadata helpers must not leave generated bytecode in the source tree.
__pycache__/
