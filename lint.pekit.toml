[allow]
"elf.cet" = "Go's internal linker does not emit x86 IBT or shadow-stack properties; CGO remains disabled so Loregd retains its closed pure-Go dependency graph"
"elf.relr" = "Go's internal linker emits PIE self-relocations as RELA and does not support DT_RELR; Loregd has no DT_NEEDED shared-library dependencies"
